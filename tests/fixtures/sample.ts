export function createServer(): void {
  return;
}

function loadConfigValue(): string {
  return "ready";
}

const runSearch = async (query: string): Promise<string> => {
  return query;
};

export class InfimiumService {}
